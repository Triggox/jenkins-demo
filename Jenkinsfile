pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Hello DevOps'
            }
        }

        stage('Checkout') {
            steps {
                echo 'Code pulled from GitHub'
            }
        }

    }
}
